import 'package:flutter/material.dart';

class FlavorTheme {
  static const kAppPrimaryColor = Color(0xFF5CB3FF);
  static const kSplashBackgroundColor = Color(0xFFFFFFFF);
  static const kButtonPrimaryColor = Colors.blueAccent;

  static ThemeData getTheme() {
    return ThemeData(fontFamily: 'Sen');
  }
}
